<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}CodeGliders User Manager{% endblock %}</title>
        {% block stylesheets %}
            <link href="{{ asset('css/style.css') }}" type="text/css" rel="stylesheet" />
        {% endblock %}
    {% block javascripts %}
    
    {% endblock %}
    <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
</head>
<body>

    <div id="header">
        <div style="position:relative;float:left">
            <div class="main-title">CodeGliders User manager</div>
            <div class="header-subtitle">Applicazione CRUD di test con il framework Symfony</div>
        </div>
        <div class="header-subtitle" style="position:relative;float:right;padding:10px;">
            Utente registrato:<span class="header-subtitle" style="font-weight:bold;padding-right:20px;">
                {{ app.user.username }}
            </span><br>
            <a href="{{ path('user_change_pwd') }}" style="font-weight:bold;text-decoration:none;color:#fff">Cambio password</a>&nbsp;&nbsp;&nbsp;
            <a href="{{ path('logout') }}" style="font-weight:bold;text-decoration:none;color:#fff">Logout</a>
        </div>
    </div>
        {% block navbar %}
              <div id="navbar">
        <br>
        <br>
        <ul>
            <li><a href="{{ path('user_splash') }}">Home</a></li>
            <li><a href="{{ path('usergrid') }}">Elenco utenti</a></li>
            <li><a href="{{ path('adduser') }}">Aggiungi utente</a></li>
             <li><a href="{{ path('rolegrid') }}">Elenco ruoli</a></li>
             <li><a href="{{ path('addrole') }}">Aggiungi Ruolo</a></li>
               <li><a href="{{ path('groupgrid') }}">Elenco Gruppi</a></li>

        </ul>

    </div>
        {% endblock %}
  
    <div id="content"> 
        {% block body %}
        {% endblock %}
    </div>







</body>
</html>
